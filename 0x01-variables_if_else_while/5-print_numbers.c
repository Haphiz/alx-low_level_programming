#include <stdio.h>
/**
 * main - Entry point
 * Description: list numbers less than 10
 *
 * Return: 0 if Success
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	return (0);
}
