#include "main.h"

/**
 * _abs - prints absolute value of a number
 * @n: integer type n
 * Return: absolute value of n
 */
int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
